Architecture/block flow diagram. Appears at the start of the "Description" section, giving a one-line visual summary of the use case's overall flow: "The use case flow begins after a resource order for an ODA Component and ends with a newly created instance on an ODA Canvas."

Three items left to right, connected by two grey chevron/arrow shapes pointing right:

1. **Resource Order Management TMFC011** — a hexagon icon divided into four colored triangular segments (blue, green, yellow, red, grey), the standard TM Forum ODA component icon.
2. **Resource Configuration and Activation TMFC062** — the same four-color ODA hexagon icon.
3. **ODA Canvas** — a rounded-rectangle container (grey border) containing the same four-color ODA hexagon icon, representing the deployed instance running on the ODA Canvas.

The diagram simply shows the flow direction: a resource order handled by Resource Order Management (TMFC011) leads to activation work by Resource Configuration and Activation (TMFC062), which results in an instance running on the ODA Canvas.
