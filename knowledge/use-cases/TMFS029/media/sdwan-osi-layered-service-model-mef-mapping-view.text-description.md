Architecture/block diagram (Figure 3.1.2, "SD-WAN service model mapped to refactored IG1373 SD-WAN OSI Layered Service Model"), a second view derived from the same underlying "Gliffy master diagram" as `sdwan-osi-layered-service-model-with-underlay-clouds.png` and the canonical `sdwan-osi-layered-service-model-canonical-full.png` (the source document notes this diagram is "a specific view of the Gliffy master diagram in the appendix", produced by showing/hiding drawing layers).

The overall layout — Management/Control/User/Infrastructure planes, the lettered A–H key, the "e2e Service Management Operational Domain" with the "Service Management Intelligent Controller (SMIC)" box, the "SD-WAN Operational Domain" with Provider Edge Router / Virtual Provider Edge Router / SDWAN Controller and SWVC/TVC/UCS connections, and the "Physical and Line Plant Technology Domain" boxes at the bottom — is identical to `sdwan-osi-layered-service-model-with-underlay-clouds.png`. The difference in this view is that the middle "Control Plane L2" band, which in the other view shows the "IP Underlay Service" box with the IPVC Controller and the MPLS/internet/cellular cloud icons, is left blank here — this view is filtered to focus on the SD-WAN service-model mapping (the layers immediately above and below the SD-WAN virtual connection) and hides the IP/underlay-transport layer of detail shown in the companion figure.
