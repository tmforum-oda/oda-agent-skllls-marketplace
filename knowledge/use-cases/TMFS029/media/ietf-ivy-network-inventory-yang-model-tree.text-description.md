Code/schema image (Figure 3.5.4, "Draft IETF Network Inventory model in YANG"), a slide titled "YANG Models" (footer: "IVY WG @ IETF 121, Dublin"), showing an extract of the draft-ietf-ivy-network-inventory-topology YANG module as a text tree.

The tree augments `/ni:network-inventory` with three top-level list entries, each shown as a YANG tree fragment:

- **`optical-cable* [id]`**: leaves `id` (string), `uuid?` (yang:uuid, read-only), `name?`/`description?`/`alias?` (string), `cable-type?` (identityref), `fiber-core-num?` (uint32); a container **`a-end`** with `device-type?` (identityref) and a choice `(connected-device-type)?` between case `passive` (`device-id?` string) and case `active` (`ne-ref?` / `component-ref?`, both leafref); a mirrored container **`z-end`** with the same structure; and a nested list `optical-cable-segment* [id]` (details continued in the next column, "......").
- **`optical-cable-segment* [id]`** (right column, expanded): leaves `id`, `uuid?`, `name?`, `description?`, `alias?` (string), `fiber-core-num?` (uint32), `fiber-type?` (identityref), `length?` (uint32), `attenuation?` (decimal64), `serial-num?`/`mfg-name?`/`part-number?` (string); and its own `a-end` / `z-end` containers with the same `device-type?` / `(connected-device-type)?` passive-or-active choice structure as `optical-cable`.
- **`passive-device* [id]`**: leaves `id`, `uuid?` (read-only), `name?`, `description?`, `alias?` (string), `device-type?` (identityref), `custom-tags*` (string), `location-ref?` (nil:ni-location-ref); and a nested list `passive-port* [id]` with leaves `id`, `uuid?` (read-only), `name?`, `description?`, `alias?` (string), `port-type?` (identityref), `fiber-core-num?` (uint32).

This is a textual YANG schema tree (not a UML class diagram) describing the candidate IETF network-inventory data model — optical cables, optical cable segments, and passive devices/ports, each end-referenced to either a passive device-id or an active network-element/component leafref — that the document proposes as a possible basis for a formal SID information model.
