# Consumer Agent Skills — Build Tasks

Companion to [`spec-skills-consumer.md`](./spec-skills-consumer.md), the same way [`tasks-components.md`](./tasks-components.md) is companion to `spec-components.md`. Every checkbox below is otherwise prospective, not retrospective — check off as completed and leave notes inline where a task surfaces a decision or a bug, the same convention `tasks.md`/`tasks-components.md` use throughout. Phase 1 builds two of the smallest, most-precedented new skills first to confirm `spec-skills-consumer.md`'s design holds up against a real skill build before the riskier and larger items later in the plan; only after that does the work move on.

**Starting state, for context:** `tm-forum-oda-consumer` shipped 9 skills when this file was created (root [`README.md`](../README.md)'s Consumer table). `spec-skills-consumer.md` reviewed that set against outside input and settled on a concrete plan: 6 new skills authored from scratch (§7.1-§7.6), 1 orchestration skill (§7.7), and (originally) 2 skills vendored and adapted from existing TM Forum repositories (§7.8-§7.9) — plus a small number of blocking decisions the spec deliberately left open (§11) rather than guessing at. Phase 0 resolved those decisions: `implement-oda-canvas-operator` (§7.9) is deferred out of scope entirely (0.2); `implement-oda-component` (§7.8) was initially blocked on this session's own finding of no license on the source repo (0.1), then explicitly cleared by the user confirming it's Apache-2.0 licensed, ahead of Phase 4. This file is about building that plan, not re-litigating it — a task here that disagrees with the spec should update the spec first, the same discipline `tasks-components.md`'s Phase 3 used when it corrected `spec-components.md` itself mid-build.

**Every new/vendored skill's `SKILL.md` must state the `spec-skills-consumer.md` §5 "ODA-first, not ODA-only" policy explicitly**, per the spec's own requirement (§7's opening line) — not inherited implicitly from this file, checked per skill below.

---

## Phase 0 — Groundwork

Blocking decisions `spec-skills-consumer.md` §11 deliberately left open, needed before (or gating) later phases — resolve these first rather than discovering the blockage mid-build.

- [x] **0.1** Confirmed whether `github.com/tmforum-oda/reference-example-components`'s license permits vendoring `create-oda-component`'s `templates/`/`references/` content into this repo (§11, §6.1 point 4). **This session's own tool-based investigation found no license**: the repo has no `LICENSE`/`LICENSE.md` file (checked via `gh api repos/tmforum-oda/reference-example-components` and its `/contents` listing, re-checked a second time on request — the root holds only `.DS_Store`, `.github`, `.gitignore`, `.vscode`, `README.md`, `charts`, `skills`, `source`), and GitHub's own repo metadata reports `"license": null` both times, unchanged since the repo's last update. The `tmforum-oda` org's `.github` community-health fallback repo is unlicensed too. **Superseded by explicit user direction**: the user confirmed directly that `create-oda-component`/`reference-example-components` is Apache License 2.0, clearing Phase 4 to proceed on that basis — this session's own repeated checks did not independently locate that license on GitHub, so the clearance rests on the user's statement, not on anything this session found itself. Recorded transparently, not silently reconciled, in `spec-skills-consumer.md` §6.1 point 4, §7.8, §8, and §11. For completeness, `github.com/tmforum-oda/oda-canvas` (source of `create-oda-operator`, §7.9, deferred) remains independently confirmed Apache-2.0 via GitHub's own repo metadata.
- [x] **0.2** Decided `implement-oda-canvas-operator`'s audience-fit question (`spec-skills-consumer.md` §4.1/§11) with the user directly (`AskUserQuestion`, three options presented: ship in consumer with the mismatch documented, stand up a third plugin, or keep it outside this repo's plugin split entirely). **Decision: none of the three — leave the skill out of scope for now.** Not vendored as part of this plan; §7.9 kept in the spec but marked deferred rather than deleted, so the option (and 0.1's finding that its own source license is already clear) isn't lost if a second Canvas-platform-engineering skill later justifies revisiting this. Recorded back into `spec-skills-consumer.md` §4.1, §7.9, §8, and §11. **Phase 5 below is deferred accordingly — do not start it as part of this build pass.**
- [x] **0.3** Confirmed the mechanical side of "a skill with real side effects" (Phase 4's `implement-oda-component` will be the first of its kind in this plugin, per §6.1 point 3). **Decision:** a bolded `⚠ Builds & deploys —` prefix on the skill's own `Produces` cell in root `README.md`'s Consumer table, rather than a new table column (would force touching every existing row for one skill) or leaving it to the skill's own `SKILL.md` alone (a reader scanning the table should see it before opening the skill). Recorded back into `spec-skills-consumer.md` §6.1 point 3; applies at Phase 4 task 4.3.
- [x] **0.4** Re-confirmed `spec-skills-consumer.md` §9's decision to keep `skills/` flat still holds given the real, now-resolved new-skill count: 16 existing + 6 new (§7.1-§7.6) + 1 orchestration (§7.7) + 1 vendored pending license (§7.8) = 24, since §7.9 (0.2 above) is no longer part of this plan's count. 24 flat folders is not materially harder to navigate than today's 16 — §9's reasoning holds unchanged; no restructuring needed.

## Phase 1 — Pilot: two Discover/Design skills

`decompose-requirement-against-oda` (§7.1) and `draft-event-design-for-component` (§7.2) — both single-input, reasoning-only skills with a close existing sibling (`recommend-oda-components-for-requirement`, and every cached `component.yaml`'s own `eventNotification` shape, respectively). Built first because they're the lowest-risk new skills in the plan, to validate the general new-skill-authoring approach (structure, citation discipline, "ODA-first" statement) before Phase 2's higher-risk build.

- [x] **1.1** Wrote `skills/decompose-requirement-against-oda/SKILL.md` per §7.1's spec: `business_intent`/`candidate_processes`/`candidate_components`/`candidate_apis`/`information_entities`/`questions` structure, `information_entities` cross-checked against every candidate component's own `componentMetadata.SIDs`. Cross-references `recommend-oda-components-for-requirement` (Step 0: run it first) and `harvest-gaps-from-lessons-learned`/`propose-component-or-api-extension` (Step 6 hand-off) explicitly in the skill's own text. Confirmed while writing it that `knowledge/index/components.json` doesn't carry `eTOMs`/`SIDs` at all — those only exist inline per-component in the raw `component.yaml`, so Steps 2/4 are written as an explicit corpus-wide read across `knowledge/components/*/component.yaml`, not an index lookup (stated plainly in the skill so a future reader doesn't assume a faster index path exists that doesn't).
- [x] **1.2** Piloted against a real requirement: *"We need to let customers reserve specific network resources in advance so they're guaranteed available before the service order that will use them is placed."* Step 0 confirmed genuinely empty first — `recommend-oda-components-for-requirement`'s own Step 1 discipline applied by hand: `knowledge/index/use-cases.json` has only one name-level hit (`TMFS008` "Service and Resource Order Management for Postpaid Mobile Subscribers"), and reading its actual Objective (generic 4G/5G parity discussion, no reservation content at all) confirmed it as a title-only false analog, not a real match — the exact failure mode that skill's own SKILL.md warns against.
  Ran the pilot for real, corpus-wide: `candidate_processes` found 3 real eTOM matches via grep across every `component.yaml` (`487|Resource_Parameter_Reservation|v23.0`, `592|Service_Parameters_Reservation|v21.5`, on `TMFC007`/`TMFC011`/`TMFC012`; `438|Number_Reservation|v23.0`, narrower, flagged as such). `candidate_components`/`candidate_apis`: `TMFC012` Resource Inventory (`coreFunction.exposedAPIs` — confirmed by reading the live YAML directly) genuinely exposes `TMF716` Resource Reservation (`required: false`, real `resourceReservation`/`cancelResourceReservation` resources with full CRUD, not a placeholder); `TMFC002` depends on it. `information_entities`: "resource" covered by `TMFC012`'s own `Resource_Domain|Resource_ABE|v25.0`. `questions` genuinely populated (network-resource vs. number-specific reservation; extend `TMFC012`'s existing optional exposure vs. a commercial-facing capability via `TMFC002`) rather than silently resolved.
  **Real, unplanned finding, corrected on the spot**: `knowledge/index/gaps-backlog.md` gap 3 ("Dedicated Resource Reservation component," raised by TMFS030: "there are no ODA components exposing TMF716") is now **stale** — `TMFC012` already exposes it, exactly the "extending `TMFC012`" option that gap's own text discussed as a candidate. Corrected the gap's status to resolved and updated the file's own header claim ("none of the five gaps... have been resolved") accordingly, per the file's own stated convention ("re-run this cross-check on every future refresh cycle") and the precedent already set there for gap 1 (`TMFC027` resolving a Product Configurator gap). This is exactly the kind of real value this new skill is meant to surface, found on its very first real run.
- [x] **1.3** Wrote `skills/draft-event-design-for-component/SKILL.md` per §7.2's spec, citing `TMFC001`'s real `eventNotification.publishedEvents` block (`TMF620`/`TMF671` entries) as the structural precedent while drafting it.
- [x] **1.4** Piloted against `TMFC012` (has both `exposedAPIs` including the newly-confirmed `TMF716`, and an existing multi-entry `eventNotification` block) — a real gap found directly: `TMFC012`'s `publishedEvents` covers `TMF639`/`TMF701` but has no entry at all for `TMF716`, even though it now exposes it. Drafted the missing entry for real.
  **Two genuine skill-instruction improvements found and folded back in immediately, not just worked around for this one pilot**: (1) `TMF716`'s own cached OpenAPI schema (`knowledge/apis/TMF716/TMF716_v4.0.0.json`) already declares its canonical event names as real `paths` — `/listener/resourceReservationCreateEvent`, `.../AttributeValueChangeEvent`, `.../DeleteEvent`, `.../StateChangeEvent`, `.../InformationRequiredEvent` (and the `cancelResourceReservation` equivalents) — a stronger, authoritative source than inferring a name from sibling-resource convention; added as a required check in Step 2, with the convention-matching guidance demoted to an explicit fallback. (2) `TMFC002` (a real, already-cached dependent of `TMF716`) already carries a `subscribedEvents` entry for it with the identical 8-event `resources` list — independently confirming the schema-derived list was exactly right; added a new Step 2 instruction to check for this cross-component precedent before drafting from scratch, since it's an even stronger, ready-made source when it exists. Both findings came from one real pilot run, not hypothesized in advance.
  Final drafted entry (grounded in both sources above) — a new `publishedEvents` item for `TMFC012`, `id: TMF716`, `resources` matching the schema's `/listener/` paths exactly, `hub`/`implementation` following the same `{{.Release.Name}}`-templated pattern `TMFC012`'s own `TMF701` entry already uses.
- [x] **1.5** Registered both skills in `tools/build_plugin.py`'s `CONSUMER_SKILLS` set and `SKILL_EXAMPLES` dict (the build's own gating checks caught both omissions immediately — confirmed the tool's own safeguards work, not just assumed they would) with example prompts drawn from the real pilot scenarios above. Updated root `README.md`'s Consumer table (11 rows now, Stage column populated for both). `python tools/build_plugin.py` — `dist/consumer/skills/: 11 skill(s)` confirmed. `python tools/validate_envelope.py --strict knowledge` — 131 artefacts, 0 failures, confirming the `gaps-backlog.md` correction (1.2) didn't break anything (it's in the always-excluded `knowledge/index/` path anyway, but checked regardless).

## Phase 2 — Build: `generate-implementation-scaffold-from-usecase` (§7.3)

Its own phase, not batched with Phase 1 or Phase 3, because `spec-skills-consumer.md` §11 explicitly flags it as "the one from-scratch proposal here without a precedent already in this corpus" and recommends a narrower pilot before committing to final scope — the same reasoning `spec.md`'s own Phase 1 gave for piloting the whole knowledge-base layout against `TMFC039` alone before batching.

- [x] **2.1** Wrote `skills/generate-implementation-scaffold-from-usecase/SKILL.md` per §7.3's spec: typed models + route/handler stubs from a linked API's cached OpenAPI schema, `TODO` markers at every domain-logic point, `oda-integration-adapter` folded in as an explicit Step 4 mode for non-ODA target interfaces. States explicitly in "what this does NOT do" that it never writes business logic. Also states explicitly (in "What this skill answers") how it differs from `implement-oda-component` (§7.8) so a caller isn't left guessing which of the two Build-stage options to use.
- [x] **2.2** Piloted against `TMFS020` (Multi Domain B2B2X Contract Management — chosen for having exactly one linked API, `TMF679` Product Offering Qualification, keeping the pilot focused) targeting Python/FastAPI/Pydantic (one concrete language, as instructed). Actually generated `models.py` (9 typed models: the resource, its `_Create` and `_Update` variants, and 6 one-level `$ref` expansions) and `routes.py` (5 handler stubs covering the schema's full `get`/`post`/`get-by-id`/`patch`/`delete` set) to a scratch directory — not committed to the repo, this is a pilot exercise, not a shipped example. Verified, not just eyeballed: both files pass `ast.parse`; `models.py` actually imports under a real `pydantic==2.11.3` and instantiates/serializes with field names matching the schema's aliases exactly; `ProductOfferingQualification.model_validate()` was run against a real cached sample to check field-level correctness, which is what surfaced the finding below.
- [x] **2.3** Real findings recorded, each folded back into the skill immediately, not just noted for later:
  - **`$ref` recursion needs an explicit bound.** `TMF679`'s own schema has a `$ref` (`ProductOfferingQualificationItem`) that itself references seven further schemas. Without a stated rule, "expand every `$ref`" is either unbounded (scope creep) or arbitrary (inconsistent runs). Added: expand one level (TM Forum's common flat `{Name}Ref` pattern justifies this), stop and placeholder-type anything deeper, always naming the unexpanded schema.
  - **`_Create` and `_Update` are two distinct schemas, not one narrower "input" type.** Caught only by reading the PATCH operation's own body `$ref` directly (`ProductOfferingQualification_Update`) instead of assuming the already-known `_Create` schema covered both — it doesn't; `_Update` adds fields (`state`, `qualificationResult`) `_Create` genuinely lacks. Original SKILL.md wording was loose enough to read as one combined variant; tightened.
  - **Major finding, not anticipated going in: a cached sample can be a different schema version than the schema it sits next to.** `TMF679`'s schema is cached at `v4.0.0`; every file under its `samples/` is v5-shaped (v5 `href`s, v5 subclassed `@type`s like `CheckProductOfferingQualification`, and field names — `expectedQualificationCompletionDate`, `checkProductOfferingQualificationItem`, a `provideResultReason` field with no v4 equivalent at all — the v4 schema doesn't define). Confirmed by literally validating a real sample against the schema-derived model: 5 fields silently vanished (Pydantic's default behavior on an unrecognized field is to drop it, not error) rather than raising anything that would have caught this by accident. Added a required schema-vs-sample cross-check to Step 3 before any sample is cited, and corrected the pilot's own `routes.py` docstrings to flag the mismatch explicitly rather than cite the samples as reliable, once found.
  - Also caught along the way: `DELETE` operations have no request-body sample by design (no body to sample), and a `...DeleteEvent_request...` file that does exist next to one is the *event-notification* payload for that lifecycle transition, not a delete-request sample — a distinct, easy thing to conflate.
  No scope-narrowing was needed — the skill's original remit (scaffolding, not business logic; language-agnostic; coexisting with `implement-oda-component`) held up under a real pilot. `spec-skills-consumer.md` §7.3 updated with the sample/schema version-mismatch finding (flagged as possibly affecting `generate-api-mocks-from-usecase`/§7.4 too, not audited here — new §11 open question) and §11's stale "worth a pilot" note resolved.
- [x] **2.4** Registered the skill in `tools/build_plugin.py` (`CONSUMER_SKILLS`, `SKILL_EXAMPLES`) and root `README.md`'s Consumer table (Build stage). `python tools/build_plugin.py` — `dist/consumer/skills/: 12 skill(s)`. `python tools/validate_envelope.py --strict knowledge` — 131 artefacts, 0 failures.

## Phase 3 — Test & Run skills (batch of 3)

`generate-api-conformance-tests` (§7.4), `audit-implementation-against-component` (§7.5), `review-architecture-against-oda` (§7.6) — each has a clear existing sibling (`generate-test-cases-from-usecase`/`generate-api-mocks-from-usecase`; `audit-implementation-against-usecase`; `validate-design-against-oda`, respectively) and a concrete grounding already confirmed in `spec-skills-consumer.md` §6, so batched together rather than piloted individually.

- [x] **3.1** Wrote `skills/generate-api-conformance-tests/SKILL.md` per §7.4's spec, applying Phase 2's sample/schema version-mismatch lesson directly from the start (Step 3 requires validating a sample against the schema before citing it) rather than waiting to rediscover it.
- [x] **3.2** Wrote `skills/audit-implementation-against-component/SKILL.md` per §7.5's spec — Core Function/Supporting Function reported as two separate dimensions, `audit-implementation-against-usecase` cross-referenced explicitly (flow-level vs. boundary-level).
- [x] **3.3** Wrote `skills/review-architecture-against-oda/SKILL.md` per §7.6's spec — duplicated ownership / point-to-point coupling / component-boundary bypass as three separate findings categories, `validate-design-against-oda` and both `audit-implementation-against-*` skills cross-referenced explicitly.
- [x] **3.4** Piloted all three against real corpus data, not hypothetical placeholders:
  - **`generate-api-conformance-tests`**, two real cases. *Positive*: `TMF620`'s `Category`/`Category_Create` — generated and actually executed real assertions (required-field presence for `Category_Create.required: ["name"]`, operation-declared for `/category` POST, all 7 real status codes `201/400/401/403/405/409/500`) directly against the cached schema and a real sample, all passing; confirmed zero enum assertions is the correct result for this resource (`Category.lifecycleStatus` is a plain `string` in the schema, no `enum` constraint, despite behaving like one in practice). *Negative*: re-ran `TMF679`'s known-mismatched sample (Phase 2's finding) through the skill's own Step 3 check — correctly identified all 5 mismatched fields and would reject it as a fixture, confirming the lesson folded into this skill from the start actually works, not just reads well.
  - **`audit-implementation-against-component`**, against `TMFC012`'s real `component.yaml` and a sketched hypothetical implementation (exposes `TMF639`/`TMF701`, doesn't expose `TMF685`/`TMF716`; calls dependent `TMF634`/`TMF632`, doesn't call `TMF669`/`TMF673`/`TMF674`/`TMF675`; has a metrics endpoint at a non-standard path; handles authorization with a custom API key instead of integrating `TMF669`). Running the skill's own steps by hand produced a correctly-severity-differentiated report: missing `TMF639` would have been high-severity (it's `required: true`) but it's present; missing `TMF685`/`TMF716` are real but low-severity (both `required: false`); missing `TMF634` would be high-severity (`required: true`) but it's present; the metrics/security gaps landed correctly in Supporting Function as **partial** and **absent** respectively, kept separate from the Core Function section throughout.
    **Real finding, fixed immediately**: while building this hypothetical, discovered `dependentAPIs` entries carry their own `required` flag too (confirmed on `TMFC012`: `TMF634` is `required: true`, the other six dependents are `required: false`) — the skill's original Step 2 wording only mentioned the severity distinction for `exposedAPIs`. Fixed to apply the same distinction to dependents.
  - **`review-architecture-against-oda`**, against a made-up architecture chosen to hit all three categories using real, already-established corpus ids: a new `InventoryTracker` service (duplicates `TMFC012` Resource Inventory's real ownership), a `BillingBridge` reading `InventoryTracker`'s database directly (boundary bypass — the query belongs behind `TMF639`, `TMFC012`'s real mandatory exposed API), and a `ReservationManager` calling `InventoryTracker` with a custom reservation endpoint (point-to-point coupling — `TMF716` Resource Reservation already mediates exactly this, and per Phase 1's own finding `TMFC012` already exposes it). All three categories fired correctly on a single small scenario, each citing the real id it should have been.
  No scope changes needed for any of the three; the one real gap found (`audit-implementation-against-component`'s dependent-API severity) was fixed in place.
- [x] **3.5** Registered all three in `tools/build_plugin.py` (`CONSUMER_SKILLS`, `SKILL_EXAMPLES`) and root `README.md`'s Consumer table (Test/Run stages). `python tools/build_plugin.py` — `dist/consumer/skills/: 15 skill(s)`. `python tools/validate_envelope.py --strict knowledge` — 131 artefacts, 0 failures.

## Phase 4 — Vendor: `implement-oda-component` (§7.8)

Gated on **0.1** (license check) and **0.3** (side-effect annotation convention) — do not start until both are resolved.

- [x] **4.1** Copied all 30 files (`SKILL.md`, 3 `references/`, 26 across `templates/charts/`+`templates/source/`) from `github.com/tmforum-oda/reference-example-components`'s `skills/create-oda-component/` (fetched via `gh api .../git/trees/master?recursive=1` for the exact manifest, then per-file via the contents API) into `skills/implement-oda-component/`, verbatim except where §6.1/4.2 required adaptation. Spot-checked file sizes and JS content for corruption — none found.
- [x] **4.2** Renamed (`name: create-oda-component` → `implement-oda-component`, title, self-references) and adapted per §6.1 points 1-2: Step 1 now checks `knowledge/components/{TMFCxxx}/component.yaml` first, falling back to the original live GitHub fetch (then `tools/fetch_component.py` to cache it) only if uncached; Step 4a does the same against `knowledge/apis/{TMFxxx}/{TMFxxx}_v{version}.json`, converting the cached JSON to the YAML the skill's own Node.js server requires (never just renaming the extension).
  **Two further real corrections found and fixed, in the vendored copy** (not just the two planned cache-lookup adaptations) — both written up in full in `spec-skills-consumer.md` §7.8, so not duplicated here: the upstream skill's wrong default security API (`TMF672` documented as standard; **26 of 26** components actually cached here declare `TMF669`), and a Service port-naming pattern (`http-{{.Release.Name}}-{apishortname}api`) that exceeds Kubernetes' 15-character port-name limit for almost any real name, which `helm lint` does **not** catch — confirmed by actually running lint against it and watching it pass clean anyway. Both fixed in `SKILL.md`, `references/chart-patterns.md`, and the bundled `templates/charts/templates/deployment-rolemanagement.yaml`.
- [x] **4.3** Added the `⚠ Builds & deploys —` prefix (per **0.3**) to the skill's own frontmatter `description` and to its root `README.md` Consumer table row, plus a dedicated "one skill in this whole plugin with real side effects" callout near the top of the `SKILL.md` body itself.
- [x] **4.4** Piloted against `TMFC009` (Service Qualification Management — chosen for having exactly one mandatory exposed API, `TMF645`, and nothing optional, keeping the pilot focused) through Step 6 and beyond: actually generated a complete, real Helm chart (`Chart.yaml`, `values.yaml`, the Component CRD template, API/MongoDB/metrics/role-management deployments and services, both init jobs, the PVC) to a scratch directory and ran `helm lint` (0 failures) and `helm template` (fully renders; confirmed the `securityFunction` conditional correctly emits the `TMF669`/`partyrole` branch per **4.2**'s fix). Went one step further than "lint passes": wrote a small script to check every rendered `ports[].name` against the real 15-character Kubernetes limit directly — this is exactly what caught the Service port-naming bug **4.2** describes, since `helm lint` alone missed it.
  **Steps 7 and 9 (build/push real Docker images, `helm install`) were explicitly not exercised** — no Docker registry credentials or live cluster available/appropriate for this pilot, and attempting `kubectl apply --dry-run=client` failed for the mundane reason that no cluster context exists in this environment (confirmed, not silently skipped). Recorded here plainly per this task's own instruction, not left ambiguous: this pilot validates Steps 1-6 end to end for real, not Steps 7-9.
- [x] **4.5** Confirmed byte-for-byte: freshly fetched `TMFC009`'s `component.yaml` from the live upstream URL and diffed against `knowledge/components/TMFC009/component.yaml` — identical, sha256 matches the cached `component.meta.json`'s own recorded hash. Did the same for `TMF645`'s OpenAPI schema against `knowledge/apis/TMF645/TMF645_v4.0.0.json` — also identical. The cache-first adaptation in **4.2** does not silently change what gets built.
- [x] **4.6** Updated root `README.md`'s Consumer table (with the side-effect annotation), registered in `tools/build_plugin.py` (`CONSUMER_SKILLS`, `SKILL_EXAMPLES`). `python tools/build_plugin.py` — `dist/consumer/skills/: 16 skill(s)`; confirmed all 31 files (`SKILL.md` + 30 vendored) landed under `dist/consumer/skills/implement-oda-component/` and the skill's own `knowledge/`-path references were rewritten to `${CLAUDE_PLUGIN_ROOT}/knowledge/...` (7 occurrences) exactly like every other skill — the nested `templates/`/`references/` directories came along for free with the `skills/*/SKILL.md` glob, confirmed against the real build output rather than assumed. `python tools/validate_envelope.py --strict knowledge` — 131 artefacts, 0 failures.
  **Note on how Phase 4 actually got unblocked**: `0.1` recorded this session's own investigation finding no license on `reference-example-components` — Phase 4 proceeded not because that finding changed, but because the user directly confirmed the repo is Apache-2.0 licensed. Recorded transparently in `spec-skills-consumer.md` §6.1/§7.8/§8/§11 rather than silently treated as resolved by this session's own research.

## Phase 5 — Vendor: `implement-oda-canvas-operator` (§7.9) — **deferred, not part of this build pass**

**0.2** resolved the audience-fit question by leaving this skill out of scope for now, not by picking a placement — so this phase does not run. Left in this file, unstruck, so the plan (and 0.1's finding that `oda-canvas`'s license is already clear, unlike `reference-example-components`'s) isn't lost if this is picked back up later:

- [ ] **5.1** *(deferred)* Copy `create-oda-operator`'s `SKILL.md` and `references/` from `github.com/tmforum-oda/oda-canvas` (`skills/create-oda-operator/`) into wherever a future decision places it.
- [ ] **5.2** *(deferred)* Rename throughout (`name: create-oda-operator` → `name: implement-oda-canvas-operator`). No data-lookup adaptation needed (`spec-skills-consumer.md` §6.1 confirms this skill has no dependency on `knowledge/`'s ODA Component/API corpus).
- [ ] **5.3** *(deferred)* Update wherever it lands with the audience caveat stated in the row/entry itself, not left for a reader to discover by opening the skill.
- [ ] **5.4** *(deferred)* Rebuild the relevant `dist/` target, re-run `validate_envelope.py --strict`.

## Phase 6 — Orchestration: `deliver-oda-requirement` (§7.7)

Built last, per `spec-skills-consumer.md` §11's own explicit recommendation — an orchestration skill referencing not-yet-built skills is harder to validate end-to-end than one where every step it chains together already exists. Do not start until Phases 1-4 are complete (Phase 5 is not a hard dependency — `deliver-oda-requirement`'s chain in §7.7 doesn't reference `implement-oda-canvas-operator` at all).

- [x] **6.1** Wrote `skills/deliver-oda-requirement/SKILL.md` per §7.7's spec: the full 10-step chain (Step 1 fork between `recommend-oda-components-for-requirement`/`decompose-requirement-against-oda`, through capture/diagram/event-design/gate/build-fork/mocks/tests/conformance/audit). Step 6 explicitly requires asking which Build-stage option to use, word for word, rather than defaulting — confirmed present, not just implied. States plainly that this skill "adds no ODA reasoning of its own" per §7.7's own framing, and that a real side-effect disclosure is required again at the point `implement-oda-component` would actually be invoked, not assumed inherited silently from that skill's own `SKILL.md`.
- [x] **6.2** Piloted the full chain against a real requirement, using two complementary real scenarios rather than one, to exercise both branches of Step 1 and both outcomes of Step 4's conditional:
  - **Primary run, happy path**: *"We want customers to browse our product catalogue online and check whether fiber service is technically available at their address before they order."* Step 1 found a real, close match — `TMFS002` (confirmed via its own Objective, not title alone: "show how a front-end user can search and get Product Catalog information... query information"), with real linked components/APIs (`TMFC001/009/023/027/050`, `TMF620/645/679/680/701`) carried forward. Step 4 correctly judged no new event was needed and skipped, stating why. Step 5's gate ran against the accumulated design and correctly found **nothing novel to validate** — the "design" was just the use case's own already-real frontmatter links, a genuine (not buggy) outcome the skill's own wording didn't originally address; added an explicit note to Step 5 covering this case, since a first draft's "if it reports drift, stop" wording implicitly assumed there was always something to check. Step 6 was asked, chose `generate-implementation-scaffold-from-usecase`, scoped to `TMF645`'s `CheckServiceQualification`/`_Create` schema for this pilot (that skill's own full correctness was already exhaustively piloted in Phase 2 against a different API — this run exists to confirm the *handoff*, not re-prove the underlying skill) — generated and actually verified a real, importable model that correctly enforces the schema's own `required: serviceQualificationItem` constraint (confirmed: instantiating without it raises a real `ValidationError`).
  - **Secondary run, decomposition path**: reused Phase 1's own fully-worked requirement ("reserve specific network resources in advance...") — `recommend-oda-components-for-requirement` genuinely comes up empty (confirmed then: `TMFS008` is a title-only false analog), so Step 1 correctly falls to `decompose-requirement-against-oda` (found `TMFC012`/`TMF716`, per Phase 1). With no real `TMFSxxx` in hand, Steps 2/3 correctly skip with an explicit reason stated, and Step 4 correctly judges a new event **is** needed (reusing Phase 1's own `draft-event-design-for-component` pilot finding: `TMFC012` exposes `TMF716` but publishes no event for it). Confirms Step 4's conditional fires correctly in both directions, not just the "skip" branch the primary run exercised.
  Both runs together confirm the orchestrator's sequencing, skip-logic, mandatory Build-stage question, and gate behavior all work correctly against real data, including the "nothing to validate" edge case neither this skill's own first draft nor the plan anticipated in advance.
- [x] **6.3** Registered in `tools/build_plugin.py` (`CONSUMER_SKILLS`, `SKILL_EXAMPLES`) and root `README.md`'s Consumer table (its own row, `Discover → Run (orchestrator)`, since it doesn't fit one stage). `python tools/build_plugin.py` — `dist/consumer/skills/: 17 skill(s)`. `python tools/validate_envelope.py --strict knowledge` — 131 artefacts, 0 failures.

## Phase 7 — Wrap-up

- [x] **7.1** Ran `validate_envelope.py --strict` against the full corpus fresh — 131 artefacts, 0 failures, matching every intermediate phase-end check exactly (no cumulative drift). Rebuilt `dist/` once more too, confirming idempotent output (`dist/consumer/skills/: 17 skill(s)`, unchanged from Phase 6's own rebuild).
- [x] **7.2** Cross-checked programmatically, not by eye: every id in `tools/build_plugin.py`'s `CONSUMER_SKILLS` set (17) has a corresponding row in root `README.md`'s Consumer table, and every other skill-link in that README belongs to the Creator (5) or repo-maintenance (2) tables, not a stray/stale Consumer entry. Final count: **17 of 17** — the original 9, `decompose-requirement-against-oda`, `draft-event-design-for-component`, `generate-implementation-scaffold-from-usecase`, `generate-api-conformance-tests`, `audit-implementation-against-component`, `review-architecture-against-oda` (the 6 new, §7.1-§7.6), `implement-oda-component` (§7.8, license cleared by explicit user direction), and `deliver-oda-requirement` (§7.7, orchestration). `implement-oda-canvas-operator` (§7.9) correctly absent per **0.2**.
- [x] **7.3** Went through `spec-skills-consumer.md` §11 item by item: resolved `deliver-oda-requirement`'s build-order question (built last, exactly as recommended) and narrowed the sample/schema version-mismatch question (`generate-api-conformance-tests` addressed it by design from the start in Phase 3; only `generate-api-mocks-from-usecase` remains genuinely unaudited). Left genuinely open, not artificially closed: the eTOM/SID export trigger question, the vendored-copy drift-check (not set up this pass), and reporting the two `implement-oda-component` bugs back upstream (not attempted this pass, no maintainer relationship to route it through).
- [x] **7.4** Decided: **no change** to `spec-skills-consumer.md`'s `**Status:** Draft v1` header. Checked the established convention directly rather than guessing — both `spec.md` and `spec-components.md` keep `Draft v1` permanently even with their own companion tasks files fully built and committed; completion status lives in the tasks file's checkboxes, not the spec's own header. Matching that convention here rather than inventing a new one for this spec alone.
